apiVersion: v1
kind: Service
metadata:
  name: {{ include "envforge.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "envforge.fullname" . }}-backend
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
